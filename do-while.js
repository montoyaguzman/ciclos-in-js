const matters = [10, 5, 10, 9, 8, 10];
const matters2 = [];

// valor inicio
let i = 0;
do {
  console.log('Bienvenido...');
  console.log(matters2[i]);
  i++; // razon de cambio
} while(matters2.length > 0 && i < matters2.length); // condicion